# Section Normalization

Preferred structure for SKILL.md:

1. Purpose
2. When to Use (if needed)
3. Workflow (steps)
4. Reference Files (Load On Demand)
5. On-demand (optional)

Remove or avoid:

- Separate `References` + `Load Order` sections
- `Integration` sections unless required by behavior
- Duplicate success criteria across multiple skills
